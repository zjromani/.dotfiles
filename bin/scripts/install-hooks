#!/usr/bin/env bash
# Point git at the tracked hooks directory for this repo.
# Run once after cloning: bin/scripts/install-hooks
set -euo pipefail

REPO_ROOT="$(git rev-parse --show-toplevel)"
git config core.hooksPath hooks
echo "Git hooks installed (core.hooksPath=hooks)"
